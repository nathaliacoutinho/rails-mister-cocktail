<div class="container">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description, placeholder: "1 tbs" %>
  <%= f.input :ingredient_id, collection: Ingredient.all, prompt: "choose one" %>
  <%= f.submit "add dose", class: "btn btn-primary btn-sm" %>
<% end %>
</div>
